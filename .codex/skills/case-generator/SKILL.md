---
name: case-generator
description: 将Excel格式测试用例批量转换为极简YAML格式。智能列名匹配(中英文多别名)。参数 source(必填:.xlsx文件路径), environment(可选:默认环境名)。
---

# case-generator — Excel→YAML

**立即调用** `mcp__agent-for-web-ui-testing__case-generator`：
- `source`: 用户指定的 .xlsx 路径
- 如果用户提供了环境名，加上 `environment`

不要做额外确认，直接展示结果。
