---
name: web-explore
description: 一键探索Web页面结构并生成增强版Acc Tree YAML。参数 url(必填:页面URL), mode(可选:quick单页/deep递归同域,默认quick)。
---

# web-explore — 探索页面结构

**立即调用** `mcp__agent-for-web-ui-testing__web-explore`：
- `url`: 用户指定的 URL
- 如果用户要求深度探索，加上 `mode: "deep"`

不要做额外确认，直接展示结果。
