---
name: web-snapshot
description: 获取当前页面增强Acc Tree快照(DOM+A11y+几何+定位器+交互事件)。参数 compact(可选:true=紧凑模式)。
---

# web-snapshot — 增强快照

**立即调用** `mcp__agent-for-web-ui-testing__web-snapshot`：
- 如果用户要求紧凑模式，加上 `compact: true`

不要做额外确认，直接展示结果。
